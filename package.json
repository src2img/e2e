{
  "name": "vue-sample",
  "version": "0.0.0",
  "scripts": {
    "clean-sources": "rimraf public src index.html",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5050",
    "start": "serve --single --no-clipboard --listen ${PORT:-8080} dist"
  },
  "dependencies": {
    "serve": "^14.1.2"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.2.0",
    "rimraf": "^3.0.2",
    "vite": "^3.2.5",
    "vue": "^3.2.45"
  }
}
